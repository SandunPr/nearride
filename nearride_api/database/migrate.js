const fs=require('fs'),path=require('path'),mariadb=require('mariadb');const env=require('../src/config/env');(async()=>{const sql=fs.readFileSync(path.join(__dirname,'schema.sql'),'utf8');const connection=await mariadb.createConnection({...env.db,multipleStatements:true,database:undefined});try{await connection.query(sql);console.log('Database schema applied.')}finally{await connection.end()}})().catch(e=>{console.error(e.message);process.exit(1)});
