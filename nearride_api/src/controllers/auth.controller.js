const bcrypt=require('bcrypt'),jwt=require('jsonwebtoken'); const {OAuth2Client}=require('google-auth-library'); const {v4:uuid}=require('uuid'); const Joi=require('joi'); const {query}=require('../config/database'); const env=require('../config/env'); const tokens=require('../services/token.service'); const {ok,AppError}=require('../utils/http');
const safe=u=>({publicId:u.public_id,email:u.email,fullName:u.full_name,avatarUrl:u.avatar_url,phone:u.phone,isCustomer:!!u.is_customer,isProvider:!!u.is_provider,isAdmin:!!u.is_admin});
async function register(req,res){const {value,error}=Joi.object({email:Joi.string().email().required(),password:Joi.string().min(8).max(72).required(),fullName:Joi.string().min(2).max(120).required(),phone:Joi.string().max(30).allow('',null)}).validate(req.body,{abortEarly:false});if(error)throw new AppError(422,'Validation failed.','VALIDATION_ERROR',error.details.map(x=>({field:x.path.join('.'),message:x.message})));const existing=await query('SELECT id FROM users WHERE email=? LIMIT 1',[value.email.toLowerCase()]);if(existing.length)throw new AppError(409,'An account with this email already exists.','EMAIL_EXISTS');const id=uuid();const result=await query('INSERT INTO users (public_id,email,password_hash,full_name,phone,created_at,updated_at) VALUES (?,?,?,?,?,NOW(),NOW())',[id,value.email.toLowerCase(),await bcrypt.hash(value.password,12),value.fullName,value.phone||null]);const user=(await query('SELECT * FROM users WHERE id=?',[result.insertId]))[0];ok(res,{user:safe(user),session:await tokens.session(user,req.get('user-agent'))},'Account created successfully.');}
async function login(req,res){const {email,password}=req.body||{};const rows=await query('SELECT * FROM users WHERE email=? AND deleted_at IS NULL LIMIT 1',[String(email||'').toLowerCase()]);const user=rows[0];if(!user||!user.password_hash||!(await bcrypt.compare(String(password||''),user.password_hash)))throw new AppError(401,'Invalid email or password.','INVALID_CREDENTIALS');if(user.account_status!=='active')throw new AppError(403,'This account is not active.','ACCOUNT_SUSPENDED');await query('UPDATE users SET last_login_at=NOW() WHERE id=?',[user.id]);ok(res,{user:safe(user),session:await tokens.session(user,req.get('user-agent'))},'Signed in successfully.');}
async function refresh(req,res){let payload;try{payload=jwt.verify(req.body?.refreshToken,env.jwt.refreshSecret);}catch{throw new AppError(401,'Invalid refresh token.','INVALID_REFRESH_TOKEN');}const digest=tokens.hash(req.body.refreshToken);const rows=await query('SELECT rt.id,u.* FROM refresh_tokens rt JOIN users u ON u.id=rt.user_id WHERE rt.token_hash=? AND rt.revoked_at IS NULL AND rt.expires_at>NOW() LIMIT 1',[digest]);if(!rows[0])throw new AppError(401,'Refresh token is revoked or expired.','INVALID_REFRESH_TOKEN');await query('UPDATE refresh_tokens SET revoked_at=NOW() WHERE token_hash=?',[digest]);ok(res,{session:await tokens.session(rows[0],req.get('user-agent'))},'Session refreshed.');}
async function logout(req,res){if(req.body?.refreshToken)await query('UPDATE refresh_tokens SET revoked_at=NOW() WHERE token_hash=?',[tokens.hash(req.body.refreshToken)]);ok(res,null,'Signed out successfully.');}
async function me(req,res){const rows=await query('SELECT * FROM users WHERE public_id=? AND deleted_at IS NULL',[req.auth.sub]);if(!rows[0])throw new AppError(404,'User not found.','NOT_FOUND');ok(res,safe(rows[0]));}
async function google(req,res){if(!env.googleClientId)throw new AppError(503,'Google sign-in is not configured.','GOOGLE_AUTH_NOT_CONFIGURED');const ticket=await new OAuth2Client(env.googleClientId).verifyIdToken({idToken:req.body?.idToken,audience:env.googleClientId});const p=ticket.getPayload();let user=(await query('SELECT * FROM users WHERE google_id=? OR email=? LIMIT 1',[p.sub,p.email]))[0];if(!user){const r=await query('INSERT INTO users(public_id,email,google_id,full_name,avatar_url,is_email_verified,created_at,updated_at) VALUES(?,?,?,?,?,1,NOW(),NOW())',[uuid(),p.email,p.sub,p.name||p.email,p.picture]);user=(await query('SELECT * FROM users WHERE id=?',[r.insertId]))[0];}else if(!user.google_id)await query('UPDATE users SET google_id=? WHERE id=?',[p.sub,user.id]);ok(res,{user:safe(user),session:await tokens.session(user,req.get('user-agent'))},'Google sign-in successful.');}
module.exports={register,login,refresh,logout,me,google};
