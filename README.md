# NearRide MVP

- `nearride_api/` — standalone Express + MariaDB service.
- `nearride_app/` — standalone Flutter Material 3 application.

See each project README for setup. NearRide is a discovery marketplace, not a transport operator or ride-hailing service.
