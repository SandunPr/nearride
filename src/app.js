const express=require('express'),helmet=require('helmet'),cors=require('cors'),compression=require('compression'),morgan=require('morgan'),rateLimit=require('express-rate-limit');const env=require('./config/env');const routes=require('./routes');const {notFound,errorHandler}=require('./middleware/error');
const app=express();app.disable('x-powered-by');app.use(helmet({crossOriginResourcePolicy:{policy:'cross-origin'}}));app.use(cors({origin(origin,cb){if(!origin||env.corsOrigins.length===0||env.corsOrigins.includes(origin))return cb(null,true);cb(new Error('Origin not allowed by CORS'));},credentials:true}));app.use(compression());app.use(morgan('combined',{skip:()=>env.nodeEnv==='test'}));app.use(express.json({limit:'1mb'}));app.use(express.urlencoded({extended:false,limit:'1mb'}));app.use('/uploads',express.static(env.uploadDir));app.get('/health',(req,res)=>res.json({success:true,message:`${env.appName} API is healthy.`,data:{environment:env.nodeEnv}}));app.use('/api/v1',rateLimit({windowMs:60_000,limit:120}),routes);app.use(notFound);app.use(errorHandler);module.exports=app;
